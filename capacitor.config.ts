import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  // appId: 'io.ionic.starte'
  appId: 'com.poncube.shop',
  appName: 'poncube',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
